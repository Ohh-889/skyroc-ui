Skyroc UI

一套现代化、优雅、简洁的 React 组件库。

```ts copy
git clone https://github.com/Ohh-889/skyroc-ui
```

```js show-line-numbers
{
  '/proxy-default': {
    target: 'https://default.com',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/proxy-default/, ''),
  },
  '/proxy-demo': {
    target: 'https://demo.com',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/proxy-demo/, ''),
  }
}
```

```tsx
import React from 'react'

export default function Demo() {
  return <div className="text-muted-foreground text-sm">Hello Live JSX</div>
}
```

<InstallDependencies pkg="soy-ui" />
